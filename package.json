{
  "name": "sistema-cf2",
  "version": "0.1.0-stage0",
  "private": true,
  "description": "CF 2.0 — Etapa 0: contratos y fixtures; sin runtime productivo.",
  "type": "module",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "validate": "node scripts/validate-contracts.mjs",
    "test": "node --test",
    "check": "node scripts/lint.mjs && node scripts/validate-contracts.mjs && node --test",
    "migrate:prod": "node scripts/migrate-prod.mjs",
    "start:prod": "node src/prod-service.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
